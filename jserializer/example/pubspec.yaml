name: example
description: A starting point for Dart libraries or applications.
version: 1.0.0
publish_to: none

environment:
  sdk: '>=3.7.0 <4.0.0'

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  build_runner: ^2.10.0
  jserializer_generator:
    path: ../../jserializer_generator

dependencies:
  flutter:
    sdk: flutter
  meta: ^1.15.0
  benchmark_harness: ^2.0.0
  benchmark: ^0.3.0
  pure: ^0.1.1
  json_annotation: ^4.9.0
  json_serializable: ^6.9.0
  fpdart: ^1.2.0
  jserializer:
    path: ../
  equatable: ^2.0.5

